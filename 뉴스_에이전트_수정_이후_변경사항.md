# 뉴스 에이전트 수정 이후 변경사항

작성일: 2026-08-30
대상: `/home/agent2/FINAL` 운영 파이프라인

## 1. 정리 범위

이 문서는 뉴스 수집 기간과 전처리 방식을 재설계한 시점부터 최종 보고서의 차트 설명을 수정한 시점까지의 변경사항을 정리한 것이다. 중간에 검토했던 안이 아니라 현재 `FINAL` 코드에 반영된 운영 구조를 기준으로 작성했다.

## 2. 뉴스 수집·전처리

초기의 일별 요약과 상위 10건 제공 방식은 3개월 단위 분석에 맞춘 주간 처리 방식으로 변경했다.

현재 처리 순서는 다음과 같다.

```text
기준일 직전 90일 뉴스 수집
→ URL 중복 제거
→ 7일 수집 구간 및 같은 달력 주간 안에서 의미가 유사한 기사 병합
→ 기사 사건과 기업보고서 문장 간 임베딩 유사도 계산
→ 주간별 임베딩 상위 15건 선정
→ 교차 인코더 재정렬
→ 주간별 최대 5건 확정
→ 주간 요약 생성
→ 주별 확정 사건의 합집합에서 전체 상위 20건 선정
```

- 의미 중복 판단의 코사인 유사도 기준은 `0.88`, 시간 범위는 `168시간`으로 설정했다.
- 여러 날짜의 기사가 하나의 사건으로 병합되면 대표 기사만 남기지 않는다. 날짜별로 기업보고서와 가장 관련성이 높은 제목 한 건을 골라 `날짜·제목` 형식의 진행 내역을 시간순으로 보존한다.
- Google News에서 수집되는 제목과 스니펫을 사용하며 기사 전체 본문은 수집하지 않는다. 본문 수집에 따른 입력량 증가를 피하면서도 사건의 핵심 내용을 유지하기 위한 선택이다.
- URL은 수집·중복 제거 단계에서만 사용하고 하위 에이전트의 보조자료에는 전달하지 않는다.
- News Agent 입력은 `3개월 주간 요약`과 `기업 관련 뉴스 상위 20건`으로 구성한다. 주간 요약은 기간 전체의 흐름을 파악하는 문맥이며, 뉴스 분석 문장의 직접 근거는 상위 20건의 원 뉴스 식별자로 제한한다.
- 뉴스 사건의 재무 영향은 확인된 자료 범위에서만 해석한다. 뉴스 발생 전 재무제표를 후행 사건의 성과 확인 자료처럼 사용하지 않는다.

주요 설정은 [`configs/news_default.yaml`](configs/news_default.yaml), 처리 계약은 [`src/Agent_Team/News_Agent/README.md`](src/Agent_Team/News_Agent/README.md)에 정리되어 있다.

## 3. 하위 에이전트 간 보조자료와 분석기간

뉴스 자료가 다른 에이전트에 전달되는 방식도 함께 수정했다.

- Financial Agent와 YFinance Agent에는 News Agent가 작성한 주장이나 상위 20건 전체를 전달하지 않고, 90일 주간 요약을 보조자료로 제공한다.
- Financial Agent는 공시를 주 분석자료로 유지하면서 주간 뉴스 요약을 사업 환경과 해석 범위를 보완하는 자료로 사용한다.
- YFinance Agent는 30거래일이 아니라 뉴스와 같은 90일 분석기간의 시장 자료를 사용한다. 이동평균 등 지표 계산에 필요한 이전 자료는 내부 준비 구간으로만 추가 수집한다.
- YFinance Agent는 주간 뉴스 흐름과 주가·거래량·시장 대비 성과의 시간적 대응을 분석하되, 동시 발생만으로 인과관계를 단정하지 않는다.
- News Agent도 재무·시장 보조자료를 사건의 직접 증거로 사용하지 않고 배경과 한계 설명에만 활용한다.

이 구조는 하위 에이전트가 다른 에이전트의 결론을 재사용하는 방식이 아니라, 같은 원천에서 정리된 보조자료를 각자의 주 분석자료와 함께 해석하도록 한 것이다.

## 4. 공시 수집과 재무 수치 처리

- 기업보고서가 수집되지 않았던 초기 실행은 OpenDART 시스템 점검 시간과 겹친 것이 원인이었다. 서비스 재개 후 대상기업과 비교기업의 공시 수집을 포함한 전체 실행을 다시 확인했다.
- 괄호 표기와 앞쪽 음수 기호를 모두 음수로 인식하도록 재무 숫자 해석을 보완했다.
- 비용 계정은 비율 계산과 독자 표시에서 양의 비용 규모로 정규화하고, 이익·손실 계정은 원래 회계 부호를 유지한다. 이에 따라 음수로 표시된 비용 때문에 마진 방향이 뒤집히는 문제를 방지했다.
- 비교 수치의 단위, 기준일과 비교기간을 구조화해 서로 다른 기준의 값을 직접 비교하지 않도록 했다.

## 5. 언어모델 설정

- 최종 운영 파이프라인의 기본 모델을 `gpt-5.4`로 통일했다.
- 논문 실험에서 생성 비용을 줄여야 할 때는 실행 스크립트가 `gpt-5.4-mini`를 명시하도록 구분했다.
- `FINAL`은 운영용 패키지이므로 기본 실행과 현재 최종 검증 결과는 모두 `gpt-5.4`를 사용한다.

## 6. 에이전트 운영 원칙과 게이트 제거

기존에 검토했던 A·B·C 게이트는 최종 운영 경로에서 제거했다. 정해진 규칙이 자연어 판단의 방향을 바꾸거나 재생성을 지시하면 에이전트의 역할이 규칙 기반 판정기로 축소될 수 있다는 판단에 따른 것이다.

현재 Python 코드가 담당하는 범위는 다음과 같다.

- 자료 수집, 계산, 단위 변환과 구조화
- 허용된 근거 식별자와 출처 연결
- JSON 형식, 중복, 존재하지 않는 근거 참조 등 구조적 무결성 확인
- 표의 확인된 수치·사실과 고정 면책문구 작성
- 차트 생성과 HTML 렌더링

반면 근거의 중요도, 투자 방향, 위험의 의미와 보고서 문장은 하위 분석 에이전트, Strategy Agent와 Writer Agent가 입력 자료에 근거해 작성한다. 운영 실행에서는 자연어 내용을 규칙으로 채점해 결론을 바꾸거나 같은 요청을 자동 재호출하지 않는다.

## 7. Strategy Agent 개편

Strategy Agent는 하위 에이전트의 분석 결과와 구조화된 근거를 받아 대상기업에 대한 현재 판단을 작성하도록 개편했다.

- `Buy`, `Hold`, `Sell`과 같은 정형화된 의견 등급은 v5 운영 산출물과 최종 보고서에서 사용하지 않는다.
- 기존 편입자와 신규 자금을 구분한다. 기존 편입자에게는 비중 확대·유지·축소 중 현재 대응을, 신규 자금에는 즉시 진입·분할 진입·진입 유보 중 현재 대응을 명시한다.
- 단순 사실 나열을 줄이고 재무, 뉴스, 가격과 비교기업 근거가 현재 행동에 어떤 의미가 있는지 작성한다.
- 재검토를 전제로 판단을 미루지 않는다. 기준일에 하위 에이전트가 제공한 자료만으로 현재 결론을 완결한다.
- 판단 한계는 `최근 분기 미반영`, `비교기업 한 곳`처럼 설계상 당연한 사실을 반복하지 않는다. 핵심 논거의 해석을 실제로 제한하는 누락 자료, 기간 불일치 또는 확인되지 않은 연결 관계를 설명한다.
- 결론에 필요한 근거만 최대 6개까지 선택하며, 재무·뉴스·시장·비교기업 항목을 의무적으로 하나씩 채우지 않는다.

과거의 `decision_basis_card.json`은 더 이상 생성하거나 사용하지 않는다. 현재는 다음 세 요소가 역할을 나누어 담당한다.

- `strategy_decision_output_v5.json`의 `decision_basis_cards`: 판단에 직접 사용한 근거와 역할
- `report_context_cards`: 보고서 문장에서 실제 참조한 보충 문맥
- `strategy_packet_provenance_v2.json`: 원자료 식별자와 파일 경로

자세한 계약은 [`src/Agent_Team/Strategy_Agent/README.md`](src/Agent_Team/Strategy_Agent/README.md)에 정리되어 있다.

## 8. 비교기업 처리

비교기업은 지표 몇 개만 수집하는 별도 축약 경로가 아니라 대상기업과 같은 하위 분석 절차를 거친다.

```text
대상기업: Financial → News → YFinance
비교기업: Financial → News → YFinance
                   ↓
동일 기준 비교 데이터셋 및 비교 분석
                   ↓
Strategy가 대상기업 판단에 필요한 비교 근거만 선택
```

- 비교 분석은 재무, 뉴스와 시장 자료를 모두 종합한다.
- 동일 지표·단위·기간·기준일이 확인된 값만 직접 비교한다.
- 비교기업은 별도의 투자 평가 대상이나 보고서 섹션으로 노출하지 않는다.
- 비교 결과는 대상기업의 판단을 보강하거나 수정하는 경우에만 사용한다.
- 비교기업 한 곳의 결과를 업종 평균이나 산업 전체의 특성으로 일반화하지 않는다.
- 최종 보고서에는 Strategy가 선택한 최대 2개 비교 축과 축별 최대 2개 지표만 포함할 수 있다.

## 9. Writer Agent와 보고서 내용

최종 보고서가 중립적인 정보 요약에 머물지 않도록 Strategy와 Writer의 역할을 분리해 보완했다.

- Strategy가 판단 방향, 근거, 반대 논리와 위험을 작성하고 Writer가 이를 6개 보고서 항목으로 편집한다.
- 투자 판단 요약에서 기존 편입자와 신규 접근자의 대응을 명확히 구분한다.
- 주요 이벤트는 뉴스 제목이나 원문 스니펫을 그대로 출력하지 않고, 확인된 사건과 대상기업 판단에 미치는 의미를 연결해 작성한다.
- 수급·모멘텀도 내부 인자명이나 원시 JSON을 그대로 노출하지 않고 수익률, 이동평균 대비 위치, 거래량과 상대성과를 독자가 이해할 수 있는 한국어 문장으로 표현한다.
- 핵심 근거표는 `확인된 수치·사실`과 `투자 해석`을 분리한다. 수치는 구조화 자료에서 가져오고 해석은 Strategy가 선택한 근거 범위 안에서 작성한다.
- Python이 생성한 투자 문장을 보고서에 삽입하지 않는다. Python은 수치 표시, 표 구성, 내부 식별자 제거와 렌더링만 담당한다.
- 보고서 하단에는 다음 문구를 가운데 정렬한 작은 글씨로 고정한다.

> 본 리포트는 투자 판단을 위한 참고자료이며, 최종 투자 결정과 그에 따른 책임은 투자자 본인에게 있습니다. 불확실성과 변동 가능성을 충분히 고려하여 보수적인 관점에서 접근하시기 바랍니다.

Writer의 현재 구성은 [`src/Agent_Team/Writer Agent/WRITER_AGENT_WORKFLOW.md`](src/Agent_Team/Writer%20Agent/WRITER_AGENT_WORKFLOW.md)에 정리되어 있다.

## 10. 차트 선택과 설명

Visualization Agent와 Writer Agent의 역할을 다음과 같이 정리했다.

1. Visualization Agent가 실제 산출물로 만들 수 있는 차트 목록과 차트별 확인 수치를 구성한다.
2. Writer Agent가 Strategy의 판단 근거와 연결되는 차트를 최대 2개 선택한다.
3. Visualization Agent는 Writer가 요청한 차트만 PNG와 PDF로 생성한다.
4. Writer Agent가 같은 호출에서 차트 관찰과 투자 판단상 의미를 작성한다.

현재 제공 가능한 차트는 다음 7종이다.

- 주가·이동평균·거래량
- 대상기업과 코스피의 지수화 성과
- 공헌이익률과 판관비율 추이
- 매출·공헌이익·판관비 추이
- 대상기업과 비교기업의 기간 수익률
- 대상기업과 비교기업의 수익성
- 대상기업과 비교기업의 유동성·자본구조

최근 수정에서는 차트 아래의 고정 설명문을 독자에게 표시하지 않도록 했다. `주가는 이동평균선 대비 위치와 거래량 비율을 함께 보여준다`와 같은 문장은 차트 제작 정보로만 남고, 최종 보고서에는 다음 두 문장이 표시된다.

- `chart_observation`: `chart_facts`에서 직접 확인되는 날짜·기간·수치
- `investment_interpretation`: 연결된 Strategy 근거가 대상기업 판단에 미치는 의미

차트 선택 이유인 `selection_reason`은 내부 검증용이며 보고서에 노출하지 않는다. 이미지는 PNG를 Base64로 HTML 안에 포함해 파일 하나만 열어도 차트가 표시되도록 했다.

## 11. 실행 결과와 디렉터리 구조

최종 운영 코드만 `/home/agent2/FINAL`에 분리했고, 이 디렉터리만으로 설치와 실행이 가능하도록 구성했다.

- 설치 패키지명: `financial-agent-final`
- 실행 명령: `financial-report`
- 실험 결과, 단일 모델 비교 산출물과 불필요한 과거 실행 파일은 최종 패키지에서 제외했다.
- `.env`와 실행 결과 디렉터리는 Git 추적 대상에서 제외했다.

산출물은 에이전트별 최상위 폴더가 아니라 대상기업별로 모은다.

```text
Output_total/
└── 고려아연/
    ├── report_고려아연.html
    ├── Financial/20251031/
    ├── News/20251031/
    ├── Y_Finance/20251031/
    ├── Competitor/20251031/
    ├── Strategy/20251031/
    ├── Visualization/20251031/
    ├── Writer/20251031/
    ├── runs/20251031/
    └── 비교기업/POSCO홀딩스/
        ├── Financial/20251031/
        ├── News/20251031/
        ├── Y_Finance/20251031/
        └── runs/20251031/
```

비교기업 산출물은 독립된 최상위 기업 결과처럼 보이지 않도록 대상기업의 `비교기업` 폴더 아래에 둔다. 최종 보고서는 기업 폴더 최상위의 `report_{기업명}.html`에 저장한다.

## 12. 토큰·비용 집계

전체 실행이 끝나면 터미널 마지막에 다음 항목을 표시하도록 추가했다.

- 입력 토큰
- 캐시 입력 토큰
- 출력 토큰
- 총 토큰
- 모델별 예상 API 비용과 전체 예상 비용(USD)

같은 정보는 실행별 `llm_usage_summary.json`에 저장한다. 비용은 `gpt-5.4`와 `gpt-5.4-mini`의 입력, 캐시 입력과 출력 단가를 구분해 계산하며 도구 호출 요금과 지역 처리 추가 요금은 포함하지 않는다.

## 13. 최종 확인 결과

2026-08-30에 고려아연을 대상기업, POSCO홀딩스를 비교기업으로 두고 기준일 2025-10-31의 전체 파이프라인을 `gpt-5.4`로 실행했다.

- 대상기업 Financial·News·YFinance 실행 성공
- 비교기업 Financial·News·YFinance 실행 성공
- 비교 데이터셋과 비교 분석 생성 성공
- Strategy v5 생성 성공
- Writer 차트 선택 및 Visualization 차트 생성 성공
- 내부 HTML과 기업별 최종 HTML 생성 성공
- 실행 집계: 입력 175,369토큰, 출력 35,305토큰, 총 210,674토큰
- 당시 표준 단가 기준 예상 비용: `$0.9679975`

최종 보고서는 [`Output_total/고려아연/report_고려아연.html`](Output_total/고려아연/report_고려아연.html)에서 확인할 수 있다.

## 14. 더 이상 사용하지 않는 이전 안

현재 구조를 해석할 때 다음 항목은 사용하지 않는다.

- 일별 요약 30개와 뉴스 상위 10건을 함께 제공하는 초기안
- 뉴스 보조자료에 URL을 포함하는 방식
- YFinance의 30거래일 고정 분석기간
- A·B·C 자연어 판단 게이트
- `decision_basis_card.json`을 Visualization 또는 Writer 입력으로 사용하는 v1 방식
- Strategy 및 최종 보고서의 `Buy`, `Hold`, `Sell` 의견 등급
- 비교기업에 대한 별도 투자 의견 또는 독립 보고서 항목
- 차트 아래에 Python 고정 설명문을 독자용 해석으로 표시하는 방식
